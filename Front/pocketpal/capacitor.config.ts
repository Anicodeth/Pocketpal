import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pocketpal.app',
  appName: 'pocketpal',
  webDir: 'dist/pocketpal',
  bundledWebRuntime: false
};




export default config;
